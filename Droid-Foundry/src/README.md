These wrappers exist so PlatformIO can compile firmware that lives in the
droid folders (paths with spaces, not under `src/`).

Edit the `.cpp` file named in the wrapper, not the wrapper itself.
See `../README.md`.
